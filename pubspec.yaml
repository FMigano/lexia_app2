name: lexia_app
description: A social platform for parents and professionals to connect and share resources about dyslexia.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`.
dependencies:
  flutter:
    sdk: flutter
  # Ensure we're using compatible versions
  firebase_core: ^4.10.0
  firebase_auth: ^6.5.2
  cloud_firestore: ^6.5.0
  firebase_storage: ^13.4.2
  google_sign_in: ^7.2.0
  provider: ^6.1.1
  image_picker: ^1.0.4
  intl: ^0.20.2
  timeago: ^3.6.0
  uuid: ^4.2.2
  cached_network_image: ^3.3.0
  cupertino_icons: ^1.0.6
  google_fonts: ^6.3.2  # Add this line only
  path: ^1.8.3  # Add this line
  flutter_image_compress: ^2.1.0  # Update to the latest version
  file_picker: ^11.0.2  # Add this line
  shared_preferences: ^2.3.3
  # Remove add_2_calendar and calendar packages

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    background_color: "#1a1a2e"
    theme_color: "#1a1a2e"
  image_path: "assets/images/icon/parentpro_logo.png"
  adaptive_icon_background: "#1a1a2e"
  adaptive_icon_foreground: "assets/images/icon/parentpro_logo.png"
  adaptive_icon_foreground_inset: 32  # ✅ Increase this to add more padding (0-48)

flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/images/icon/parentpro_logo.png
